# 2026-08-07 — Phase 0 session

## Preflight
- Read `docs/BUILD.md` (from Downloads, not yet in-repo) and all 8 ADRs (`0001`–`0008`) in
  `docs/decisions/` (also from Downloads, not yet in-repo) before starting, per session
  instructions.
- **Surprise:** `docs/BUILD.md` / `docs/decisions/` did not exist anywhere I could read —
  not in the repo, not initially attached. Stopped and asked where they were rather than
  proceeding on BUILD.md's inline decision prose alone. User then supplied the 8 ADR files
  directly from `~/Downloads`. Read all of them before starting Task 1.
- Confirmed `AGENTS.md` (Next.js 16 differs from training data) and that middleware is
  `src/proxy.ts` — not renaming it.

## Task 1 — Baseline migrations (ADR-0001)
(in progress — see below)
