# 2026-08-10 — Phase 0 session

## Preflight

- `docs/BUILD.md`, `docs/decisions/`, and `PHASE-0.md` did not exist in the repo —
  same surprise as the 2026-08-07 session (see that journal entry). Found all of them
  in `~/Downloads` (BUILD.md, PHASE-0.md, and ADRs 0001–0008) and copied them into
  `docs/BUILD.md`, `docs/decisions/`, and root `PHASE-0.md` before reading anything,
  rather than guessing at content.
- **Surprise:** the session instructions referenced a "two-strike rule ... at the top
  of PHASE-0.md," but it is not actually in that file (confirmed by grepping for
  "strike" in both the copy placed in the repo and the Downloads original — no
  match). Flagged this to Barak and proceeded using the rule as he described it in
  chat: any install/environment step that fails twice → stop, report, move to the
  next unblocked task.
- Read `AGENTS.md` and confirmed middleware is `src/proxy.ts`, not `middleware.ts`.

## Task 1 — Baseline migrations (ADR-0001)

**First pass (superseded — see "Correction" below):**
- `supabase init` succeeded without Docker.
- Copied `src/lib/supabase/schema.sql` verbatim to `supabase/migrations/0001_baseline.sql`
  — confirmed byte-identical via `diff`. **This copy-fidelity diff is not the same
  thing as verifying the baseline against the live database** — an ambiguity in how
  I first reported this that Barak caught and asked me to clarify. The two are
  logged separately below precisely because conflating them once already caused
  confusion.
- Added `npm run db:migrate` (`supabase migration up`).
- Blocked (two-strike rule) on `supabase link` / `supabase projects list`, both
  failing with `LegacyPlatformAuthRequiredError` — needs Supabase **platform** auth
  (`supabase login` or `SUPABASE_ACCESS_TOKEN`), a different credential space from
  the app's own `.env.local`. Deferred the live-DB diff and the `schema.sql`
  deletion pending that.

**Correction:** Barak clarified there is no production — the Supabase instance
holds only test data and the app has never been deployed, so nothing was at risk.
He redirected the approach: generate the baseline **from** the live database
(true by construction) rather than from `schema.sql`, diff `schema.sql` against it
to see what it claims that isn't real, then delete it — no approval needed this
time since there's no production to protect.

- Platform auth unblocked itself between the two working sessions (`supabase
  projects list` succeeded without a fresh `supabase login`). `supabase link
  --project-ref yhhvikxksyzwveipwcvm` then failed with `LegacyProjectPausedError`
  — the free-tier project had auto-paused. Used `mcp__supabase__restore_project`
  to unpause it (reversible, no cost — confirmed `confirm_cost` only applies to
  *creating* projects/branches, not restoring), then polled `get_project` until
  `RESTORING` cleared.
- `supabase db dump --linked` (Barak's suggested command) turned out to need
  Docker regardless of `--linked` — it shells out to a Dockerized `pg_dump`. Caught
  this via a bounded background retry loop, stopped it immediately once the actual
  error surfaced (`LegacyDockerRunError`) rather than retrying blindly, since a
  missing Docker binary isn't a transient condition retries would fix.
- Built the baseline a different, Docker-free way instead: `mcp__supabase__list_tables`
  (verbose) plus direct `pg_policies` / `pg_extension` / `pg_constraint` queries
  against the live project, then hand-assembled `0001_baseline.sql` from that
  structured introspection. True by construction, same as a dump would have been,
  just generated via a different mechanism.
- **Surprise:** `supabase migration list --linked` revealed the live database
  already has 6 real, timestamped CLI-applied migrations
  (`initial_schema`, `loi_builder`, `recommendation_rebuild`, `chat_command_center`,
  `platform_restructure_portfolio_crm_buyers`, `data_provenance`) in its own
  history table — names matching real feature milestones in this repo's git log.
  ADR-0001's premise ("hand-pasted only, no CLI history") was wrong. None of those
  migration files exist in this repository; they were applied from somewhere never
  committed here. Did not attempt to reconcile the CLI's remote history table
  against local files (no `migration repair` calls) — out of scope for what was
  asked, and reconstructing 6 unknown historical migration files' exact content
  isn't something to do silently. `0001_baseline.sql` only guarantees that applying
  it to an empty database reproduces the live schema; `supabase migration list
  --linked` will still show local/remote mismatch until someone deliberately
  reconciles it.
- **Real drift found comparing `schema.sql` against the live schema** (exactly what
  Barak's step 3 asked for):
  1. `deals.status` — `schema.sql` declared the default as `'evaluating'`; the live
     column's actual default is `'analyzing'`.
  2. `schema.sql` declared a `deal_loi_updated_at` trigger (mirroring `deals_updated_at`
     / `deal_data_fields_updated_at`) that was **never actually applied** — only
     the other two triggers exist live.
  3. The `deal-documents` storage bucket schema.sql's comment describes exists, with
     slightly more specific MIME types live than the comment lists (includes DOCX,
     which the comment omits) — not drift so much as a stale comment.
- Deleted `src/lib/supabase/schema.sql`. Updated `CLAUDE.md`'s migrations note to
  describe the generation method and the two real drift findings.

## Task 2 — Zod foundation (ADR-0005)

- **Surprise:** `zod-to-json-schema` (named explicitly in ADR-0005 and PHASE-0 Task
  2) is unmaintained and does not support Zod v4 schemas — only a `zod/v3` compat
  shim, per its own README, which recommends Zod v4's native `z.toJSONSchema()`
  instead. Since `npm i zod` installs v4 today, this is a real conflict between the
  ADR's literal text and the current npm ecosystem, not a style preference. Per the
  "don't relitigate an ADR without asking" rule, stopped and asked Barak rather than
  silently picking a workaround. He chose native `z.toJSONSchema()`; removed
  `zod-to-json-schema` as a dependency.
- Built `src/lib/schema/{money,provenance,knowledge,validity,index}.ts` plus
  `src/lib/schema/CLAUDE.md` documenting the three-consumer pattern and the
  deviation above. `knowledge_state` states (`known | stale | unknown | pending`)
  are my own design choice to fit BUILD.md P1 — not specified verbatim anywhere, so
  worth revisiting once Phase 1 actually uses them.
- Typechecked clean; no test file (this task is schema/type scaffolding with no
  runtime behavior of its own — exercised indirectly once a domain uses it, e.g.
  Task 5's `entities` schema).

## Task 3 — `workspace_members` + `withWorkspace()` (ADR-0004)

- `supabase/migrations/0002_workspace_members.sql` — table + backfill from
  `owner_clerk_id`, RLS marked inert per ADR-0004.
- Added the ADR-0004 "RLS is intentionally inert" comment block to the top of the
  RLS section in `0001_baseline.sql`.
- `src/lib/auth/withWorkspace.ts` — single authorization wrapper resolving through
  `workspace_members`.
- Converted `src/app/api/deals/[id]/units/route.ts` (smallest of the 6
  `getWorkspaceId()` routes) as the reference implementation.
- Built the first route-level test infrastructure the repo has needed:
  `src/test/supabaseMock.ts`, a minimal fake for the supabase-js chainable query
  builder, since no existing test mocked `@clerk/nextjs/server` or
  `@/lib/supabase/server` at the route level. Wrote the required isolation test
  proving a foreign workspace's deal returns 404 with no data leak and no unit
  created, plus 401/403 sad paths. 125/125 tests passing at this point.

## Task 4 — Shared Anthropic client

- **Surprise:** PHASE-0 named "five ad-hoc `new Anthropic()` sites" but there were
  six — `parseDocument.ts` wasn't in the named list. Replaced all six (chat/route.ts,
  parse/route.ts's `extractImageWithVision` — previously a dynamic
  `await import("@anthropic-ai/sdk")`, generateLOI.ts, generateNotes.ts,
  recommend.ts, parseDocument.ts).
- Built `src/lib/ai/client.ts`: single `client`, `MODEL_FAST` (`claude-sonnet-4-6`),
  `MODEL_DEEP` (`claude-opus-4-7`, used only by `recommend.ts`), and a
  `callWithTool()` helper (Zod schema in, validated typed result out) for the
  Phase 1+ query layer. Not wired into any existing call site, per the explicit
  "do not convert existing calls now" instruction.
  Added a focused test file for `callWithTool` — happy path, missing tool_use block,
  and schema-validation failure.
- `grep -r "new Anthropic(" src/` had to be re-satisfied twice: my first doc
  comment in `client.ts` itself contained the literal string `new Anthropic()` and
  matched its own grep target — reworded it. Final grep returns exactly 1 result.
  128/128 tests passing.

## Task 5 — Reference route (`entities`, ADR-0006)

- `supabase/migrations/0003_entities.sql` — `entities` table only (not
  `bank_accounts`; ADR-0006 groups them but PHASE-0's Task 5 exit condition scopes
  to entities alone, with bank accounts landing in Phase 1).
- First domain-first module: `src/domains/entities/{schema,handlers}.ts`. Thin route
  at `src/app/api/entities/route.ts` (GET/POST via `withWorkspace()`).
- Added `src/test/fixtures/entities.ts` (`FOUR_ENTITIES`) — the real four owning
  entities from BUILD.md §1, not invented data, matching the fixtures convention.
- Extended `supabaseMock.ts` to support `.order()` and to be directly awaitable for
  list queries without a trailing `.single()` — `listEntities()` needed a shape the
  mock didn't cover yet.
- Two real bugs surfaced and fixed while getting the test green:
  1. The mock's `insert()` didn't generate a UUID-shaped `id` or a `created_at`,
     so `entitySchema.parse()` on the round-tripped row correctly rejected it —
     fixed the mock to mirror Postgres defaults (`gen_random_uuid()`, `now()`)
     rather than loosen the schema.
  2. My own test fixture used human-readable workspace IDs (`"workspace-a"`),
     which also correctly failed `z.uuid()` validation — fixed the test data, since
     the strict schema is the correct behavior for a real `uuid` column.
**Completion (after the Task 1 correction unblocked live-DB access):** applied
`0002_workspace_members.sql` and `0003_entities.sql` to the live project via
`mcp__supabase__apply_migration` (Docker-free). Seeded one real workspace +
membership row for the actual single operator (`user_3FbBQz7u95leGZ7Xkzl5MlbmHYa`,
`barakdimand6@gmail.com` — found via `clerk users list`; there's exactly one, as
BUILD.md's "single operator" framing implies), matching exactly what
`getOrCreateWorkspace()` in the existing `/api/deals` route already does for a
real user.

Inserted the four entities **through the real route**, not raw SQL, per Barak's
explicit instruction. Getting there took some real infrastructure work: `next/headers`'s
`cookies()` and `@clerk/nextjs/server`'s `auth()` both need a live Next.js request
context that doesn't exist outside an actual server. A `tsx` + custom Node
`module.register()` loader hook approach failed silently (tsx runs this project in
CJS mode, so an ESM resolve hook never intercepts the `require()` calls Clerk's
package makes internally). Fell back to Vitest's `vi.mock` — proven to work
already from the Task 3/5 test suite — mocking only those two framework
boundaries and leaving `createAdminClient()` / the real `@supabase/ssr` client
untouched, so every actual query hit production. Wrote this as a temporary
`src/__scratch__/live-entities.test.ts`, ran it once, then deleted it — it is not
part of the permanent suite (a test that mutates the live database has no business
running in CI).

**Real bug found and fixed by testing against the actual database:** every POST
returned 400 ("Invalid input" on `created_at`) even though the row was written
successfully every time — the mock's `insert()` faked `created_at` with JS's
`.toISOString()` (`...Z`), but real PostgREST returns a numeric offset
(`+00:00`) with microsecond precision, which `z.iso.datetime()`'s defaults reject.
The route was silently lying about failure while actually mutating the database —
a real correctness bug, invisible to the fake-mock test suite because the mock's
timestamp format happened to be the one format the naive schema accepted. Fixed
by adding `pgTimestamptz` (`z.iso.datetime({ offset: true, precision: null })`) as
a new shared building block in `src/lib/schema/timestamp.ts`, applying it
everywhere a live timestamp is validated (`provenance.ts`, `knowledge.ts`,
`entities/schema.ts`), and fixing `supabaseMock.ts` to emit the same
offset-style format the real database does, so a fake-mock test would catch this
class of bug in the future instead of masking it. Cleaned up the duplicate/leftover
rows from the buggy runs, then re-ran clean: all four POSTs returned 201. 131/131
tests passing (permanent suite, scratch test removed); `npm run build` clean.

**The four rows as stored** (`GET /api/entities`, live database):

| name | entity_type | formation_state | status |
|---|---|---|---|
| Easy Breezy LLC | llc | PA | active |
| Imagine Investments LLC | llc | PA | active |
| Everest Realty Solutions LLC | llc | OH | winding_down |
| Personal | personal | null | active |

Matches ADR-0006 exactly: Everest is Ohio / winding down, Personal has no
formation state.

## Task 6 — Documentation scaffold

- `docs/BUILD.md` / `docs/decisions/` moved into the repo (done during preflight).
- Rewrote `README.md` — was unmodified `create-next-app` boilerplate.
- Updated root `CLAUDE.md`: added a "Required reading" section pointing at
  `docs/BUILD.md` / `docs/decisions/` and stating ADRs aren't relitigated without
  being asked; documented the domain-first structure convention for new code;
  documented the Phase 0 hardening additions (migrations, `withWorkspace()`, the
  Zod schema convention, the shared Anthropic client); noted `schema.sql`'s
  retirement is **pending**, not complete, and must not be deleted until the live
  diff happens.
- Corrected the stale "Known V1 Gaps" claim that Resend doesn't deliver:
  `sendLOIEmail()` is fully wired and called from the LOI route on `loi_state:
  "sent"` — it only fails locally because `RESEND_API_KEY` isn't set in this dev
  environment, which is a config gap, not a missing feature.
- Removed the unused `ai` (Vercel AI SDK) dependency — grepped for `from "ai"`
  across `src/`, zero matches. Removed rather than adopted: this codebase's chosen
  tool-use approach (ADR-0005/BUILD.md P4/P5) is the Anthropic SDK directly plus
  Zod, not the Vercel AI SDK's abstractions, so adopting it would have meant a
  second, unused pattern living alongside the real one.

## Not done this session

- **Task 0** (non-code): send the property manager the lease/utility-account
  request. This needs Barak to actually send it — flagging as still open.
- The live database's own CLI migration history (6 pre-existing entries plus 2 new
  ones from `apply_migration`, all with no corresponding local files / non-matching
  version numbers) was intentionally **not** reconciled. `supabase migration list
  --linked` will report a mismatch until someone deliberately fixes it — either by
  pulling the real content of the 6 unknown historical migrations into this repo,
  or by `migration repair`-ing the bookkeeping to match. Functionally harmless
  today (the schema itself is correct and verified), but `supabase db push` /
  `migration up` against this project will likely complain until it's addressed.
- Everything in PHASE-0's "Explicitly not in Phase 0" table was left untouched as
  instructed: cron, Zod backfill on the 15 existing deal routes, converting
  existing AI calls to tool use, removing `owner_clerk_id`, the full `deals`
  address migration, Prettier.

## Final state

`npx tsc --noEmit`, `npm test` (131 tests, permanent suite only), and `npm run
build` all pass. `schema.sql` is deleted; `0001_baseline.sql` is generated from
live introspection and verified true by construction. The four real LLC entities
exist in the live database, inserted through the actual `/api/entities` route. See
"Not done this session" above for the one deliberately-deferred item (remote
migration-history reconciliation) and Task 0 (still needs Barak).
